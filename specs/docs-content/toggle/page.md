# Toggle docs page content spec

Mined from `docs/src/app/(docs)/react/components/toggle/page.mdx` only. The component's own
behavior is covered by `specs/library/toggle/behavior.md` and is referenced here by section
name instead of being restated. Demo source files under `docs/src/app/(docs)/react/components/toggle/demos/`
are out of scope for this file (Stage 2 mines them into `specs/docs-content/toggle/demos.json`).

## Page structure (headings, in order)

- `# Toggle` (h1) — `docs/src/app/(docs)/react/components/toggle/page.mdx:1`
- `## Anatomy` — `docs/src/app/(docs)/react/components/toggle/page.mdx:13`
- `## API reference` — `docs/src/app/(docs)/react/components/toggle/page.mdx:23`

No other headings exist; the page has no per-part subheadings under API reference and no
Examples section.

Non-heading page furniture, in document order:

- `<Subtitle>` — "A two-state button that can be on or off." — `docs/src/app/(docs)/react/components/toggle/page.mdx:3`
- `<Meta name="description">` — "A high-quality, unstyled React toggle component that displays a two-state button that can be on or off." — `docs/src/app/(docs)/react/components/toggle/page.mdx:4-7`
- Hero demo import and render (`./demos/hero`) before the first heading — `docs/src/app/(docs)/react/components/toggle/page.mdx:9-11`
- `TypesToggle` import for the API reference — `docs/src/app/(docs)/react/components/toggle/page.mdx:25`
- Trailing `export const metadata` SEO keywords block (11 keywords, e.g. 'React Toggle', 'Two State Button', 'ARIA Pressed Control') — `docs/src/app/(docs)/react/components/toggle/page.mdx:29-43`

## Prose claims about component behavior

The page's prose is minimal (subtitle, meta description, and a one-line Anatomy lead-in);
every prose claim found:

- Page describes the component as "A two-state button that can be on or off." This matches
  behavior.md "State model (controlled/uncontrolled, defaults, transitions)": the pressed
  state is a boolean that flips between exactly two states (`'false'` → `'true'` → `'false'`
  across clicks, mirrored by `aria-pressed`).
  `docs/src/app/(docs)/react/components/toggle/page.mdx:3`
- Meta description additionally claims the component is "unstyled" and "high-quality".
  Naming/marketing-level claim; behavior.md covers behavior only and does not address styling,
  so there is nothing to contradict.
  `docs/src/app/(docs)/react/components/toggle/page.mdx:4-7`
- Anatomy usage guidance: "Import the component and use it as a single part:", showing a bare
  `<Toggle />` imported from the `@base-ui/react/toggle` namespace. The single-part claim
  matches behavior.md "Public API surface (props, parts, subcomponents)" (no subcomponents of
  Toggle are tested; ToggleGroup is used only as a parent fixture) and "DOM structure &
  portal behavior" (output is a single native `<button>` with no wrapper).
  `docs/src/app/(docs)/react/components/toggle/page.mdx:15-21`

No usage guidance, caveats, accessibility notes, or keyboard-interaction documentation appear
anywhere in the page prose.

## API tables referenced (props/parts documented on this page)

- The `## API reference` section has no per-part subheadings; it renders a single generated
  reference component, `<TypesToggle />`, imported from `./types`.
  `docs/src/app/(docs)/react/components/toggle/page.mdx:23-27`
- `TypesToggle` is created as `createTypes(import.meta.url, Toggle)` — i.e. the reference is
  generated from the `Toggle` component itself.
  `docs/src/app/(docs)/react/components/toggle/types.ts:4`
- The autogenerated backing markdown (`types.md`) documents exactly one part, `Toggle` ("A
  two-state button that can be on or off. Renders a `<button>` element."), with a props table
  listing: `value` (unique string identifying the toggle inside a toggle group),
  `defaultPressed` (default `false`; uncontrolled counterpart of `pressed`), `pressed`
  (controlled counterpart of `defaultPressed`), `onPressedChange`
  (`(pressed: boolean, eventDetails: Toggle.ChangeEventDetails) => void`), `nativeButton`
  (default `true`; set `false` when replacing the element with a non-button via `render`),
  `disabled` (default `false`), plus the standard `className`, `style`, and `render` props.
  `docs/src/app/(docs)/react/components/toggle/types.md:7-24`
- The backing markdown also documents a data-attributes table (`data-pressed` — present when
  pressed; `data-disabled` — present when disabled) and type sections `Toggle.Props`
  (re-export), `Toggle.State` (`pressed: boolean`, `disabled: boolean`),
  `Toggle.ChangeEventReason` (`'none'`), `Toggle.ChangeEventDetails` (`reason`, `event`,
  `cancel()`, `allowPropagation()`, `isCanceled`, `isPropagationAllowed`, `trigger`), plus a
  Canonical Types alias list.
  `docs/src/app/(docs)/react/components/toggle/types.md:26-31`, `docs/src/app/(docs)/react/components/toggle/types.md:33-73`, `docs/src/app/(docs)/react/components/toggle/types.md:75-82`
- Cross-check: the single-part set matches behavior.md "Public API surface (props, parts,
  subcomponents)". The documented `pressed`/`defaultPressed` pair matches behavior.md "State
  model (controlled/uncontrolled, defaults, transitions)"; the `onPressedChange`
  `eventDetails` argument matches behavior.md "Events (names, payload shape, bubbling,
  preventDefault semantics)" (second callback argument exposes `cancel()`); `value`'s
  group-membership description matches behavior.md "Public API surface (props, parts,
  subcomponents)" (`value` as group membership identifier); `disabled` matches behavior.md
  "Accessibility (roles, aria-*, id linking)" (`disabled` + `data-disabled`,
  `aria-pressed="false"`); the "Renders a `<button>` element" claim matches behavior.md
  "DOM structure & portal behavior". No mismatch.

## Code snippets embedded directly in the .mdx (not pulled from demos/)

- One fenced code block, the Anatomy snippet (` ```jsx title="Anatomy" `): imports
  `{ Toggle }` from `@base-ui/react/toggle` and renders `<Toggle />` bare.
  `docs/src/app/(docs)/react/components/toggle/page.mdx:17-21`
- No other code blocks exist in the page. The hero renders `<DemoToggleHero />` imported from
  `./demos/hero`; its code lives in a demo file and is out of scope here (Stage 2).
  `docs/src/app/(docs)/react/components/toggle/page.mdx:9-11`

## Discrepancies (docs page vs. behavior.md)

No direct contradictions found between the page and `specs/library/toggle/behavior.md`.
Items flagged for lack of coverage or omission:

- Docs-only claim (API reference vs. behavior.md): the `nativeButton` prop (default `true`,
  with guidance to set `false` when the `render` prop renders a non-button element) is
  documented in the page's API reference, but `nativeButton` is not among the props exercised
  by behavior.md's suite ("Public API surface (props, parts, subcomponents)" lists `pressed`,
  `defaultPressed`, `onPressedChange`, `disabled`, `value`, `render`); behavior.md's
  native-button evidence comes from the conformance harness `button: true` configuration
  instead. No test asserts `nativeButton` behavior.
  `docs/src/app/(docs)/react/components/toggle/types.md:20`
- Docs-only claim (API reference vs. behavior.md): the `data-pressed` data attribute is
  documented in the page's API reference, but behavior.md "Accessibility (roles, aria-*,
  id linking)" records only `aria-pressed` mirroring (plus `data-disabled` alongside
  `disabled`); no test asserts `data-pressed`.
  `docs/src/app/(docs)/react/components/toggle/types.md:30`
- Docs-only claim (API reference vs. behavior.md): `Toggle.ChangeEventDetails` documents
  `allowPropagation()` (and `isPropagationAllowed`), but behavior.md "Events (names, payload
  shape, bubbling, preventDefault semantics)" exercises only `cancel()`; no test asserts the
  propagation-allowing behavior.
  `docs/src/app/(docs)/react/components/toggle/types.md:54-73`
- Omission (page vs. behavior.md): the page's prose contains no usage guidance for any
  behavior — no mention of the controlled/uncontrolled patterns, the `eventDetails.cancel()`
  cancellation capability, disabled behavior, or grouped usage (the latter two are documented
  only inside the generated API tables, via `disabled`/`value` descriptions and
  `Toggle.ChangeEventDetails`). behavior.md verifies all of these ("State model
  (controlled/uncontrolled, defaults, transitions)", "Events (names, payload shape, bubbling,
  preventDefault semantics)", "Edge cases (rapid interactions, unmount, nesting)"). A
  prose-vs-behavior completeness gap, not a contradiction.
  `docs/src/app/(docs)/react/components/toggle/page.mdx:3`, `docs/src/app/(docs)/react/components/toggle/page.mdx:15-21`
- Note (not a mismatch): behavior.md "Keyboard interactions" is N/A/UNVERIFIED (no test
  dispatches keyboard events) and the page likewise makes no keyboard claims — the two
  sources are silent in agreement.

## Cross-links to other docs pages

- N/A — the page contains no internal links to other Base UI documentation pages and no
  external links.
- The imports `./demos/hero` and `./types` are same-page module imports, not cross-page
  links. `docs/src/app/(docs)/react/components/toggle/page.mdx:9`, `docs/src/app/(docs)/react/components/toggle/page.mdx:25`
