# separator — docs page spec

Mined from the docs page file only:

- `docs/src/app/(docs)/react/components/separator/page.mdx` (46 lines)

Cross-checked against `specs/library/separator/behavior.md` (the component's own behavior spec, mined from its tests). That spec is cited by section name; its claims are NOT re-derived here.

## Page structure (headings, in order)

- H1 `# Separator` — `docs/src/app/(docs)/react/components/separator/page.mdx:1`
- `<Subtitle>` one-liner (not a heading, but part of the page header block): "A separator element accessible to screen readers." — `docs/src/app/(docs)/react/components/separator/page.mdx:3`
- `<Meta name="description">` for the page: "A high-quality, unstyled React separator component that is accessible to screen readers." — `docs/src/app/(docs)/react/components/separator/page.mdx:5-8`
- Hero demo render: imports `DemoSeparatorHero` from `./demos/hero` and renders it immediately after the header block — `docs/src/app/(docs)/react/components/separator/page.mdx:10-12`
- H2 `## Anatomy` — `docs/src/app/(docs)/react/components/separator/page.mdx:14`
- H2 `## API reference` — `docs/src/app/(docs)/react/components/separator/page.mdx:24`
- `export const metadata` block with SEO keywords (React Separator, Divider Component, Divider, Horizontal Rule, Horizontal Divider, Vertical Divider, Visual Separator, Section Divider, HR, Accessible Separator, Screen Reader Separator, Headless React Components, Base UI) — `docs/src/app/(docs)/react/components/separator/page.mdx:30-46`

Additional imports rendered inline mid-page (their source files are out of scope here — demo sources are Stage 2's scope):

- `import { TypesSeparator } from './types'` + `<TypesSeparator />` under API reference — `docs/src/app/(docs)/react/components/separator/page.mdx:26-28`

That is the entire page: no Usage guidelines, Examples, or other sections exist.

## Prose claims about component behavior

Only claims made by the page text itself, each cross-checked against the behavior spec (`specs/library/separator/behavior.md`).

- "A separator element accessible to screen readers." (Subtitle) — `docs/src/app/(docs)/react/components/separator/page.mdx:3`. Consistent with the behavior spec's "Accessibility" section, which proves the root renders with `role="separator"` and passes visibility checks — the screen-reader-exposed semantics the subtitle advertises.
- Meta description repeats the accessibility framing and adds "high-quality, unstyled React separator component" — `docs/src/app/(docs)/react/components/separator/page.mdx:7`. Marketing framing; "unstyled" is a styling claim with no behavioral content to check against the behavior spec, and nothing in the behavior spec contradicts it.
- Anatomy: "Import the component and use it as a single part" — `docs/src/app/(docs)/react/components/separator/page.mdx:16`. Consistent with the behavior spec's "Public API surface" section: `Separator` is a single root component with no subcomponents, parts, or context hooks exercised by the tests.
- Anatomy snippet imports `Separator` from `@base-ui/react/separator` — `docs/src/app/(docs)/react/components/separator/page.mdx:19`. Consistent with the behavior spec's "Public API surface" section, which names the same import path.
- Anatomy snippet renders `<Separator />` with no props — `docs/src/app/(docs)/react/components/separator/page.mdx:21`. Consistent with the behavior spec's "Accessibility" and "DOM structure & portal behavior" sections, where the no-props test renders a single visible `role="separator"` element in place.
- The page makes NO other behavioral claims: no prop is described in prose, no usage guidance beyond the single-part anatomy, no caveats.

## API tables referenced (props/parts documented on this page)

- The page contains NO literal Markdown API tables. The `## API reference` section consists solely of a rendered React component: `import { TypesSeparator } from './types'` + `<TypesSeparator />` — `docs/src/app/(docs)/react/components/separator/page.mdx:24-28`. The props/parts table content is generated by that component (its source is out of scope for this pass, same treatment as demo sources).
- Props/parts named in the page's prose and inline snippets: none — the only identifier appearing outside the imports is the `<Separator />` element itself (`docs/src/app/(docs)/react/components/separator/page.mdx:21`).
- N/A otherwise: no props table, no parts table, no return-value table exists in the .mdx itself.

## Code snippets embedded directly in the .mdx (not pulled from demos/)

The single `<DemoSeparatorHero />` render and `<TypesSeparator />` are component imports, not snippets, and their sources are out of scope here (demo sources are Stage 2's scope).

- **"Anatomy"** (jsx): imports `Separator` from `@base-ui/react/separator` and renders `<Separator />;` — the whole snippet is two lines, matching the "single part" framing — `docs/src/app/(docs)/react/components/separator/page.mdx:18-22`.

That is the only fenced code block on the page.

## Discrepancies (docs page vs. behavior.md)

No hard contradictions found. Scope and coverage notes:

- **Unproven-by-this-spec claims the page makes**: none beyond the accessibility framing — and that framing is consistent with the behavior spec's "Accessibility" section (`role="separator"`, visible). The "unstyled" descriptor in the meta description (`docs/src/app/(docs)/react/components/separator/page.mdx:7`) is a styling claim the behavior spec neither proves nor contradicts.
- **Silent omissions** (behavior spec covers it; docs page prose says nothing — informational only, not errors): the `orientation` prop and its `aria-orientation` reflection (behavior spec's "Accessibility" section), the `render`/`className`/`style` prop support and arbitrary DOM-prop forwarding (behavior spec's "Public API surface" and "DOM structure & portal behavior" sections), and the default HTML `div` root (behavior spec's "DOM structure & portal behavior" section). The page delegates all of this to `<TypesSeparator />` (`docs/src/app/(docs)/react/components/separator/page.mdx:26-28`), so there is nothing on the page to contradict.
- The behavior spec's "State model" section marks the default `orientation` value UNVERIFIED; the page makes no claim about any default value, so no conflict arises.

## Cross-links to other docs pages

N/A — the page body contains no Markdown links to other docs pages (no `[...](/react/...)` links, no same-page anchors). The only references are local relative imports used for rendering: `./demos/hero` (`docs/src/app/(docs)/react/components/separator/page.mdx:10`) and `./types` (`docs/src/app/(docs)/react/components/separator/page.mdx:26`), which are component imports, not docs-page links.
