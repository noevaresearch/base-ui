[ralph][library: meter] checkpoint: finish the facade-orphan fixes — MeterValue's render-function children actually receive the (formattedValue, rawValue) pair (Box<dyn Fn(&str, f64) -> AnyView + Send>, the two-argument closure spelling of MeterValue.tsx:27; the prior type claimed the pair but called render() with nothing), MeterRoot children now optional (the MeterRoot.test.tsx:216-221 no-children pin was untestable with a required Children), the garbled destructuring comment rewritten, and the wasm harness rebuilt around the new children type
