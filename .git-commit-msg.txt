[ralph][library: meter] checkpoint: the wasm suite executes in-browser for the first time and the real bugs fell out — (1) THE context-runtime split: the accessor read through reactive_graph::owner::use_context (the internals crate's rg-0.2 runtime) while MeterRoot provided through leptos::prelude::provide_context (the leptos 0.7/rg-0.1 runtime the view tree mounts under) — two independent runtimes in one process, the cross-crate owner-bridge trap the direction-provider page documents; the read now rides the same leptos runtime the tree provides under; (2) the wasm orphan-label panic test removed — a wasm panic is an uncatchable trap so catch_unwind could only ever see "some JS exception"; the contract stays pinned host-side (field_root_context.rs precedent); (3) harness/consumer-detail fixes: MeterValue subtree construction moved inside MeterRoot's children slot (eager construction ran the part body before the provider existed), the indicator probe scoped to :scope > div > div, the NVDA counter disambiguated from the also-presentation label span via [style], and the render-fn clamp pin rebuilt (raw 150 vs clamped "100%" under the default percent formatter); 11/11 wasm green in Chrome for Testing 153 + chromedriver 153, 27 host green, accordion wasm failures proven pre-existing via worktree at f8ccdb718 (identical 0/10, same panic)
