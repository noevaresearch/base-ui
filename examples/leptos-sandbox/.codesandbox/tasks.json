{
  "setupTasks": ["bash .devcontainer/setup.sh"],
  "tasks": {
    "dev-server": {
      "name": "trunk serve (wasm, live reload)",
      "command": "trunk serve --release",
      "preview": {
        "port": 3000,
        "prLink": "direct"
      },
      "runAtStart": true
    },
    "build": {
      "name": "trunk build (release)",
      "command": "trunk build --release"
    },
    "test": {
      "name": "cargo test",
      "command": "cargo test"
    }
  }
}
