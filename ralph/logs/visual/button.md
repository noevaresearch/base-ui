# Visual gap report — react/components/button

Generated by `ralph/scripts/visual-gap-report.mjs` at 2026-09-15T20:13:31.672Z.
Measured build: wasm 335 bytes, last-modified unknown.
Both sides rendered at 1280px in Chrome for Testing: upstream React docs on
http://127.0.0.1:3005, Leptos docs-app on http://127.0.0.1:3177.

**Pixel difference: 5.44%** (167055 of 3072000 px)

> **WARNING: the Leptos side rendered shell-only.** These numbers measure a page that
> did not mount its route, not a page that is merely unstyled. Fix the mount first.

- mask (white/bright = differs): `ralph/logs/visual/button-mask.png`
- overlay (upstream grey, ours red): `ralph/logs/visual/button-sbs.png`

## Prioritised gaps

- **P0 · route did not render** — this route rendered shell-only (472 chars, 0 heading(s)) even after a retry — the page content is missing entirely
  - fix: fix the mount before any styling work: run `node ralph/scripts/playwright-diff.mjs --todo-id "<id>"` to see the failure, and check for a wasm panic (the docs_app_start panic hook logs it as a console error). Fidelity numbers in this report are meaningless until it renders.
- **P0 · sidebar** — upstream renders a 258px sidebar carrying 55 links; this page has none
  - fix: port upstream's layout shell: a left column with the route tree (docs/src/app/(docs)/layout.tsx + the nav component) so every route gets the sidebar
- **P0 · syntax highlighting** — upstream's code carries 56 coloured tokens; this page has 0 — the code is unstyled monochrome
  - fix: port the highlighter (upstream pre-computes tokens via rehype/Shiki into CodeBlockPreComputed) and render the token spans with their colours
- **P0 · API reference tables** — upstream renders 1 table(s) (2 rows); this page renders 0 — props/state are prose
  - fix: render the generated types tables (name/type/default/description) over the ported types.md instead of paragraphs
- **P0 · content volume** — page text is 472 chars vs upstream 4315 (11%)
  - fix: the missing tables/affordances above are the usual cause — re-measure after they land
- **P1 · header** — upstream renders a 64px header with 4 controls; this page has none
  - fix: port the docs header (title, nav links, search surface) from docs/src/app/(docs)/layout.tsx
- **P1 · code blocks** — this page has 0 <pre> blocks vs upstream's 10 (0%)
  - fix: render the embedded snippets through the ported code-block component so each code fence becomes its own block
- **P1 · demo file tabs** — upstream renders 4 tab(s) for demo source files; this page renders none
  - fix: wrap each demo in upstream's Demo container with the react/tailwind/css-modules file selector
- **P1 · layout width** — content column 1264px vs upstream 768px
  - fix: match the docs grid so the article sits in the same column width/offset
- **P1 · fonts** — body font "Times New Roman" vs upstream "die grotesk a"
  - fix: load the docs font stack (upstream docs/src/css/index.css @theme) in the docs-app stylesheet
- **P2 · page affordances** — upstream shows 6 page actions (View as Markdown / View source / StackBlitz / StackBlitz); this page shows none
  - fix: add the page-action row (View as Markdown / View source) to the docs page shell
- **P2 · code copy control** — upstream renders 4 copy affordance(s) on code; this page renders none
  - fix: add the copy-to-clipboard control to the code-block chrome

## Measured inventory

| signal | upstream | leptos |
| --- | --- | --- |
| headings | 10 | 0 |
| sidebar | 258px / 55 links | none |
| header | 64px | none |
| links | 35 | 0 |
| code blocks | 10 | 0 |
| coloured code tokens | 56 | 0 |
| copy controls | 4 | 0 |
| demo tabs | 4 | 0 |
| tables / rows | 1 / 2 | 0 / 0 |
| page text chars | 4315 | 472 |
| article width px | 768 | 1264 |
| body font | die grotesk a | Times New Roman |

## Typography (upstream vs leptos)

| tag | upstream | leptos |
| --- | --- | --- |
| h1 | 34px/43px 700 die grotesk b | (absent) |
| h2 | 21px/26px 700 die grotesk b | (absent) |
| h3 | 18px/28px 700 die grotesk b | (absent) |
| p | 18px/28px 400 die grotesk a | (absent) |
| li | 14px/22px 400 die grotesk a | (absent) |
| code | 13px/20px 400 Paper Mono | (absent) |
| pre | 13px/20px 400 Paper Mono | (absent) |
| table | 14px/20px 400 die grotesk a | (absent) |
| a | 14px/22px 400 die grotesk a | (absent) |
